<Window x:Class="AruaRoseLoginManager.Controls.WebsiteLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Arua Website Login" Height="780" Width="1100" WindowStartupLocation="CenterScreen" Background="#202020">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" Background="#2A2A2A">
            <TextBlock Foreground="White" FontSize="13" Text="Username and password were pre-filled. Complete the human check/CAPTCHA and click Login on the site."/>
        </Border>

        <WebBrowser Grid.Row="1" Name="_browser" Navigated="Browser_Navigated" LoadCompleted="Browser_LoadCompleted"/>
    </Grid>
</Window>